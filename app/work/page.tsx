import type { Metadata } from 'next';
import Link from 'next/link';
import { ArrowUpRight } from 'lucide-react';
import { projects } from '@/data/projects';
import { site } from '@/lib/site';

export const metadata: Metadata = { title:'Selected Work', description:'Selected software, e-commerce, Shopify, UI/UX and digital growth work by Ahmed Mohyeldin.', alternates:{canonical:'/work'} };
export default function WorkPage(){return <main className="grain min-h-screen"><header className="site-nav sticky top-0 z-40" aria-label="Work navigation"><div className="container flex h-20 items-center justify-between"><Link href="/" className="display text-base" aria-label="Ahmed Mohyeldin home">AHMED MOHYELDIN<span className="text-[var(--bronze)]">.</span></Link><Link href="/#contact" className="btn btn-primary">Start a project <ArrowUpRight size={15}/></Link></div></header><section className="container py-24 md:py-36"><div className="label">Selected work</div><h1 className="display mt-6 text-[clamp(4rem,11vw,11rem)] leading-[.8]">WORK<span className="serif-italic">.</span></h1><p className="mt-10 max-w-2xl text-xl leading-relaxed text-[var(--ink-soft)]">Commercial builds, e-commerce experiences, UI/UX and digital growth — presented as visual case studies.</p></section><section className="container pb-32" aria-label="Project case studies"><div className="grid gap-8 md:grid-cols-2">{projects.map((p,i)=><Link key={p.slug} href={`/work/${p.slug}`} className="group"><div className="project-visual"><img src={p.image} alt={p.imageAlt} loading={i<2?'eager':'lazy'}/><span className="project-preview">Case study <ArrowUpRight size={12}/></span></div><div className="mt-5 flex items-start justify-between gap-4"><div><div className="text-sm text-[var(--bronze)]">{p.tag}</div><h2 className="display mt-2 text-3xl md:text-4xl">{p.name}</h2><p className="mt-3 max-w-xl text-[var(--ink-soft)]">{p.description}</p></div><ArrowUpRight className="mt-2 shrink-0 text-[var(--muted)] transition group-hover:-translate-y-1 group-hover:translate-x-1" size={20}/></div></Link>)}</div></section><footer className="border-t border-[var(--line)] py-8"><div className="container flex justify-between gap-4 text-sm text-[var(--muted)]"><span>© {new Date().getFullYear()} {site.name}</span><Link href="/#contact">Start a conversation</Link></div></footer></main>}
